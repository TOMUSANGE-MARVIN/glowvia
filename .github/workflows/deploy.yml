name: Deploy Glowvia

on:
  push:
    branches:
      - master

jobs:
  deploy:
    name: Deploy to VPS
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e

            PROJECT_PATH="${{ secrets.SSH_PATH }}"

            # ── Clone or pull ─────────────────────────────────
            if [ ! -d "$PROJECT_PATH/.git" ]; then
              git clone https://github.com/TOMUSANGE-MARVIN/glowvia.git "$PROJECT_PATH"
            else
              cd "$PROJECT_PATH"
              git pull origin master
            fi

            cd "$PROJECT_PATH"

            # ── Copy .env if not exists ───────────────────────
            if [ ! -f "$PROJECT_PATH/.env" ]; then
              cp "$PROJECT_PATH/.env.example" "$PROJECT_PATH/.env"
            fi

            # ── Update DB credentials ─────────────────────────
            sed -i "s/DB_DATABASE=.*/DB_DATABASE=${{ secrets.DB_DATABASE }}/" "$PROJECT_PATH/.env"
            sed -i "s/DB_USERNAME=.*/DB_USERNAME=${{ secrets.DB_USERNAME }}/" "$PROJECT_PATH/.env"
            sed -i "s/DB_PASSWORD=.*/DB_PASSWORD=${{ secrets.DB_PASSWORD }}/" "$PROJECT_PATH/.env"
            sed -i "s/DB_PORT=.*/DB_PORT=3307/" "$PROJECT_PATH/.env"

            # ── PHP dependencies ──────────────────────────────
            composer install --no-interaction --prefer-dist --optimize-autoloader --no-dev

            # ── Generate app key if not set ───────────────────
            php artisan key:generate --force

            # ── Frontend assets ───────────────────────────────
            cd "$PROJECT_PATH"
            npm install
            npm run production

            # ── Laravel setup ─────────────────────────────────
            php artisan migrate --force
            php artisan db:seed --force
            php artisan storage:link

            # ── Clear & cache for production ──────────────────
            php artisan cache:clear
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache

            # ── Fix permissions ───────────────────────────────
            sudo chown -R marvin:www-data "$PROJECT_PATH"
            sudo chmod -R 775 "$PROJECT_PATH/storage"
            sudo chmod -R 775 "$PROJECT_PATH/bootstrap/cache"

            # ── Reload Nginx ──────────────────────────────────
            sudo systemctl reload nginx
